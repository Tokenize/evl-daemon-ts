test("connect attempt to connect if not already connected");

test("connect should not attempt to connect if already connected");

test("send should throw error if not connected");

test("send should send given data if connected");

test("should emit disconnect event when disconnected");

test("should emit ");
